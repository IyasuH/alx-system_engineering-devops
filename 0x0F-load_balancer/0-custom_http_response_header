#!/usr/bin/env bash
# Configure Nginx so that its HTTP response contains a custom header (on web-01 and web-02)
sudo sed -i "add_header X-Served-By $hostname;" /etc/nginx/nginx.conf
sudo ervice nginx restart
