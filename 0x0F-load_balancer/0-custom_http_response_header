#!/usr/bin/env bash
# Configure Nginx so that its HTTP response contains a custom header (on web-01 and web-02)
sed -i "add_header X-Served-By 1484-web-01;" /etc/nginx/nginx.conf service nginx restart
sed -i "add_header X-Served-By 1484-web-02;" /etc/nginx/mginx.conf service nginx restart
